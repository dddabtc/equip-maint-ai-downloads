# 公开绿色演示版 poc-v2.14.3-public.1

这一版提供 Windows、macOS 和 Linux 的 x64 / ARM64 六个完整绿色包。

## 使用方式

1. 下载与你的系统和处理器匹配的压缩包。
2. 用 `SHA256SUMS.txt` 核对下载文件。
3. 解压到一个可写目录。
4. Windows 双击 `启动.cmd`；macOS / Linux 运行 `./start.sh`。
5. 浏览器打开 `http://127.0.0.1:8088`，初次使用 `admin / admin` 登录并立即改密码。

## 包含内容

- 独立 Python 运行环境和全部项目依赖
- 六层知识、本体和知识图谱
- 用户组态决策树与推理网络
- 屏蔽泵 / 磁力泵独立知识空间
- 匿名检维修规程、案例和剩余寿命演示数据
- 网页与 Word 报告
- 七种界面语言

每个平台的包都在对应原生系统上完成了解压、启动、健康检查、知识空间、决策树、案例、上传、诊断、Word 导出和重启读取测试。

公开包不包含真实客户名称、账号状态、密钥、日志、上传资料或现场运行记录。演示数据由人工构造，不用于现场设备决策。

Windows 与 macOS 包目前没有商业代码签名，系统可能显示来源或安全提示。请先核对 SHA-256 摘要，并遵守本单位的软件使用规定。

